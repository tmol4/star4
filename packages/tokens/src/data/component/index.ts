import { CIRCULAR_PROGRESS } from "./circular-progress";
import { ICON } from "./icon";
import { MATERIAL_SYMBOL } from "./material-symbols";
import { RADIO } from "./radio";
import { RIPPLE } from "./ripple";

export const COMPONENT = {
  circularProgress: CIRCULAR_PROGRESS,
  icon: ICON,
  radio: RADIO,
  ripple: RIPPLE,
  materialSymbol: MATERIAL_SYMBOL,
} as const;
